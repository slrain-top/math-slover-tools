diff --git a/CoinUtils/Makefile.am b/CoinUtils/Makefile.am
index 4a9751b..e9dae39 100644
--- a/CoinUtils/Makefile.am
+++ b/CoinUtils/Makefile.am
@@ -72,7 +72,7 @@ install-data-hook:
 	@$(mkdir_p) "$(addlibsdir)"
 if COIN_HAS_PKGCONFIG
 	PKG_CONFIG_PATH=@COIN_PKG_CONFIG_PATH@:$(DESTDIR)$(pkgconfiglibdir) \
-	"$(PKG_CONFIG)" --libs coinutils > $(addlibsdir)/coinutils_addlibs.txt
+	"$(PKG_CONFIG)" --libs coinutils > "$(addlibsdir)/coinutils_addlibs.txt"
 else
 if COIN_CXX_IS_CL
 	echo "-libpath:`$(CYGPATH_W) @abs_lib_dir@` libCoinUtils.lib @COINUTILSLIB_LIBS_INSTALLED@" > $(addlibsdir)/coinutils_addlibs.txt
diff --git a/CoinUtils/Makefile.in b/CoinUtils/Makefile.in
index 27426b4..a0c96e8 100644
--- a/CoinUtils/Makefile.in
+++ b/CoinUtils/Makefile.in
@@ -348,11 +348,11 @@ addlibsdir = $(DESTDIR)$(datadir)/coin/doc/CoinUtils
 ########################################################################
 #                         Maintainer Stuff                             #
 ########################################################################
-CLEANFILES = 
+CLEANFILES =
 
 # Files that are generated and should be cleaned with make distclean
 DISTCLEANFILES = $(am__append_3) $(VPATH_DISTCLEANFILES)
-DocFiles = README AUTHORS LICENSE 
+DocFiles = README AUTHORS LICENSE
 DocInstallDir = $(datadir)/coin/doc/$(PACKAGE_NAME)
 COIN_HAS_DOXYGEN = @COIN_HAS_DOXYGEN_TRUE@TRUE
 COIN_HAS_LATEX = @COIN_HAS_LATEX_TRUE@TRUE
@@ -841,15 +841,15 @@ uninstall-local: uninstall-doc
 
 install-data-hook:
 	@$(mkdir_p) "$(addlibsdir)"
-@COIN_HAS_PKGCONFIG_TRUE@	PKG_CONFIG_PATH=@COIN_PKG_CONFIG_PATH@:$(DESTDIR)$(pkgconfiglibdir) \
-@COIN_HAS_PKGCONFIG_TRUE@	"$(PKG_CONFIG)" --libs coinutils > $(addlibsdir)/coinutils_addlibs.txt
-@COIN_CXX_IS_CL_TRUE@@COIN_HAS_PKGCONFIG_FALSE@	echo "-libpath:`$(CYGPATH_W) @abs_lib_dir@` libCoinUtils.lib @COINUTILSLIB_LIBS_INSTALLED@" > $(addlibsdir)/coinutils_addlibs.txt
-@COIN_CXX_IS_CL_FALSE@@COIN_HAS_PKGCONFIG_FALSE@	echo -L@abs_lib_dir@ -lCoinUtils @COINUTILSLIB_LIBS_INSTALLED@ > $(addlibsdir)/coinutils_addlibs.txt
+@COIN_HAS_PKGCONFIG_TRUE@	PKG_CONFIG_PATH="@COIN_PKG_CONFIG_PATH@:$(DESTDIR)$(pkgconfiglibdir)" \
+@COIN_HAS_PKGCONFIG_TRUE@	"$(PKG_CONFIG)" --libs coinutils > "$(addlibsdir)/coinutils_addlibs.txt"
+@COIN_CXX_IS_CL_TRUE@@COIN_HAS_PKGCONFIG_FALSE@	echo "-libpath:`$(CYGPATH_W) @abs_lib_dir@` libCoinUtils.lib @COINUTILSLIB_LIBS_INSTALLED@" > "$(addlibsdir)/coinutils_addlibs.txt"
+@COIN_CXX_IS_CL_FALSE@@COIN_HAS_PKGCONFIG_FALSE@	echo -L@abs_lib_dir@ -lCoinUtils @COINUTILSLIB_LIBS_INSTALLED@ > "$(addlibsdir)/coinutils_addlibs.txt"
 
 uninstall-hook:
 	rm -f $(addlibsdir)/coinutils_addlibs.txt
 
-doxygen-docs: 
+doxygen-docs:
 	if test "$(COIN_HAS_DOXYGEN)" = TRUE; then \
 	  if test -d "doxydoc/"; then \
 	    doxygen doxydoc/doxygen.conf;\
@@ -908,14 +908,14 @@ all-doxygen-docs:
 	  if test -r $$dir/doxydoc & $$do_project = true; then \
 	    (cd $$dir ; $(MAKE) doxygen-docs) \
 	  fi ; \
-	done ; 
+	done ;
 
 clean-all-doxygen-docs:
 	for dir in $(subdirs) ; do \
 	  if test -r $$dir/doxydoc ; then \
 	    (cd $$dir ; $(MAKE) clean-doxygen-docs) \
 	  fi ; \
-	done ; 
+	done ;
 
 install-all-doxygen-docs: all-doxygen-docs
 	for dir in $(subdirs) ; do \
@@ -928,21 +928,21 @@ install-all-doxygen-docs: all-doxygen-docs
 	  if test -r $$dir/doxydoc & $$do_project = true; then \
 	    (cd $$dir ; $(MAKE) install-doxygen-docs) \
 	  fi ; \
-	done ; 
+	done ;
 
 uninstall-all-doxygen-docs:
 	for dir in $(subdirs) ; do \
 	  if test -r $$dir/doxydoc ; then \
 	    (cd $$dir ; $(MAKE) uninstall-doxygen-docs) \
 	  fi ; \
-	done ; 
+	done ;
 
-install-doc: $(DocFiles) 
+install-doc: $(DocFiles)
 	test -z "$(DocInstallDir)" || $(mkdir_p) "$(DESTDIR)$(DocInstallDir)"
 	for file in $(DocFiles); do \
 	  if test -f "$$file"; then dir=; else dir="$(srcdir)/"; fi; \
 	  if test -f "$$dir$$file"; then $(INSTALL_DATA) "$$dir$$file" "$(DESTDIR)$(DocInstallDir)/$$file"; fi; \
-	done 
+	done
 
 uninstall-doc:
 	for file in $(DocFiles); do \
diff --git a/CoinUtils/configure b/CoinUtils/configure
index 8e4bfcc..0856704 100755
--- a/CoinUtils/configure
+++ b/CoinUtils/configure
@@ -5926,7 +5926,8 @@ program_transform_name=`echo $program_transform_name | sed -f conftest.sed`
 rm conftest.sed
 
 # expand $ac_aux_dir to an absolute path
-am_aux_dir=`cd $ac_aux_dir && pwd`
+#am_aux_dir=`cd $ac_aux_dir && pwd`
+am_aux_dir=`cd $ac_aux_dir && pwd | sed "s: :\\\\\\ :g"`
 
 test x"${MISSING+set}" = xset || MISSING="\${SHELL} $am_aux_dir/missing"
 # Use eval to expand $SHELL
@@ -6851,16 +6852,20 @@ echo "$as_me: error: Cannot find the BuildTools directory" >&2;}
 fi
 
 # helpful variable for the base directory of this package
-abs_source_dir=`cd $srcdir; pwd`
+#abs_source_dir=`cd $srcdir; pwd`
+abs_source_dir=`cd $srcdir; pwd | sed "s: :\\\\\\ :g"`
 
 
 # Stuff for example Makefiles
-if test x$prefix = xNONE; then
+if test "x$prefix" = "xNONE"; then
   full_prefix=$ac_default_prefix
 else
   full_prefix=$prefix
 fi
-full_prefix=`cd $full_prefix ; pwd`
+#full_prefix=`cd $full_prefix ; pwd`
+echo "nested full_prefix: $full_prefix"
+full_prefix=`cd "$full_prefix" && pwd | sed "s: :\\\\\\ :g"`
+echo "nested full_prefix: $full_prefix"
 
 abs_lib_dir=$full_prefix/lib
 
@@ -21841,7 +21846,7 @@ if test $enable_shared = yes; then
     *-linux-*)
       if test "$GXX" = "yes"; then
         RPATH_FLAGS=
-        for dir in $abs_lib_dir; do
+        for dir in "$abs_lib_dir"; do
           RPATH_FLAGS="$RPATH_FLAGS -Wl,--rpath -Wl,$dir"
         done
       fi ;;
@@ -38596,7 +38601,7 @@ fi
 
 echo "$as_me:$LINENO: checking whether this is a VPATH configuration" >&5
 echo $ECHO_N "checking whether this is a VPATH configuration... $ECHO_C" >&6
-if test `cd $srcdir; pwd` != `pwd`; then
+if test "`cd "$srcdir"; pwd`" != "`pwd`"; then
   coin_vpath_config=yes;
 else
   coin_vpath_config=no;
@@ -38912,7 +38917,8 @@ if test x$coin_skip_ac_output != xyes; then
     echo $coin_subdirs > coin_subdirs.txt
   else
     # substitute for OBJDIR, needed to setup .pc file for uninstalled project
-    ABSBUILDDIR="`pwd`"
+    #ABSBUILDDIR="`pwd`"
+    ABSBUILDDIR="`pwd | sed "s: :\\\\\\ :g"`"
 
   fi
 
diff --git a/CoinUtils/src/Makefile.in b/CoinUtils/src/Makefile.in
index b865150..43b060f 100644
--- a/CoinUtils/src/Makefile.in
+++ b/CoinUtils/src/Makefile.in
@@ -546,7 +546,7 @@ config.h: stamp-h1
 stamp-h1: $(srcdir)/config.h.in $(top_builddir)/config.status
 	@rm -f stamp-h1
 	cd $(top_builddir) && $(SHELL) ./config.status src/config.h
-$(srcdir)/config.h.in: @MAINTAINER_MODE_TRUE@ $(am__configure_deps) 
+$(srcdir)/config.h.in: @MAINTAINER_MODE_TRUE@ $(am__configure_deps)
 	cd $(top_srcdir) && $(AUTOHEADER)
 	rm -f stamp-h1
 	touch $@
@@ -569,8 +569,9 @@ install-libLTLIBRARIES: $(lib_LTLIBRARIES)
 	@list='$(lib_LTLIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    f=$(am__strip_dir) \
-	    echo " $(LIBTOOL) --mode=install $(libLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) '$$p' '$(DESTDIR)$(libdir)/$$f'"; \
-	    $(LIBTOOL) --mode=install $(libLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) "$$p" "$(DESTDIR)$(libdir)/$$f"; \
+	    f_p=`echo "$(DESTDIR)$(libdir)/$$f" | sed "s: :\\\\\\ :g"`; \
+	    echo " $(LIBTOOL) --mode=install $(libLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) '$$p' '$$f_p'"; \
+	    $(LIBTOOL) --mode=install $(libLTLIBRARIES_INSTALL) $(INSTALL_STRIP_FLAG) "$$p" "$$f_p"; \
 	  else :; fi; \
 	done
 
@@ -590,7 +591,7 @@ clean-libLTLIBRARIES:
 	  echo "rm -f \"$${dir}/so_locations\""; \
 	  rm -f "$${dir}/so_locations"; \
 	done
-libCoinUtils.la: $(libCoinUtils_la_OBJECTS) $(libCoinUtils_la_DEPENDENCIES) 
+libCoinUtils.la: $(libCoinUtils_la_OBJECTS) $(libCoinUtils_la_DEPENDENCIES)
 	$(CXXLINK) -rpath $(libdir) $(libCoinUtils_la_LDFLAGS) $(libCoinUtils_la_OBJECTS) $(libCoinUtils_la_LIBADD) $(LIBS)
 
 mostlyclean-compile:
@@ -884,10 +885,10 @@ uninstall-am: uninstall-includecoinHEADERS uninstall-info-am \
 #######################################################################
 
 install-exec-local:
-	$(install_sh_DATA) config_coinutils.h $(DESTDIR)$(includecoindir)/CoinUtilsConfig.h
+	$(install_sh_DATA) config_coinutils.h "$(DESTDIR)$(includecoindir)/CoinUtilsConfig.h"
 
 uninstall-local:
-	rm -f $(DESTDIR)$(includecoindir)/CoinUtilsConfig.h
+	rm -f "$(DESTDIR)$(includecoindir)/CoinUtilsConfig.h"
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
 # Otherwise a system limit (for SysV at least) may be exceeded.
 .NOEXPORT:
diff --git a/configure b/configure
index 7b699d8..06b2c41 100755
--- a/configure
+++ b/configure
@@ -4853,7 +4853,8 @@ program_transform_name=`echo $program_transform_name | sed -f conftest.sed`
 rm conftest.sed
 
 # expand $ac_aux_dir to an absolute path
-am_aux_dir=`cd $ac_aux_dir && pwd`
+#am_aux_dir=`cd $ac_aux_dir && pwd`
+am_aux_dir=`cd $ac_aux_dir && pwd | sed "s: :\\\\\\ :g"`
 
 test x"${MISSING+set}" = xset || MISSING="\${SHELL} $am_aux_dir/missing"
 # Use eval to expand $SHELL
@@ -5778,16 +5779,20 @@ echo "$as_me: error: Cannot find the BuildTools directory" >&2;}
 fi
 
 # helpful variable for the base directory of this package
-abs_source_dir=`cd $srcdir; pwd`
+#abs_source_dir=`cd $srcdir; pwd`
+abs_source_dir=`cd $srcdir; pwd | sed "s: :\\\\\\ :g"`
 
 
 # Stuff for example Makefiles
-if test x$prefix = xNONE; then
+if test "x$prefix" = "xNONE"; then
   full_prefix=$ac_default_prefix
 else
   full_prefix=$prefix
 fi
-full_prefix=`cd $full_prefix ; pwd`
+#full_prefix=`cd $full_prefix ; pwd`
+echo "full_prefix: $full_prefix"
+full_prefix=`cd "$full_prefix" && pwd | sed "s: :\\\\\\ :g"`
+echo "full_prefix: $full_prefix"
 
 abs_lib_dir=$full_prefix/lib
 
@@ -20648,7 +20653,7 @@ if test $enable_shared = yes; then
     *-linux-*)
       if test "$GXX" = "yes"; then
         RPATH_FLAGS=
-        for dir in $abs_lib_dir; do
+        for dir in "$abs_lib_dir"; do
           RPATH_FLAGS="$RPATH_FLAGS -Wl,--rpath -Wl,$dir"
         done
       fi ;;
@@ -21847,7 +21852,8 @@ if test x$coin_skip_ac_output != xyes; then
     echo $coin_subdirs > coin_subdirs.txt
   else
     # substitute for OBJDIR, needed to setup .pc file for uninstalled project
-    ABSBUILDDIR="`pwd`"
+    #ABSBUILDDIR="`pwd`"
+    ABSBUILDDIR="`pwd | sed "s: :\\\\\\ :g"`"
 
   fi
 
@@ -23199,7 +23205,7 @@ if test "$no_recursion" != yes; then
 
   # Always prepend --prefix to ensure using the same prefix
   # in subdir configurations.
-  ac_sub_configure_args="--prefix=$prefix $ac_sub_configure_args"
+  ac_sub_configure_args="--prefix=\"$prefix\" $ac_sub_configure_args"
 
   ac_popdir=`pwd`
   for ac_dir in : $subdirs; do test "x$ac_dir" = x: && continue
@@ -23335,7 +23341,8 @@ echo "$as_me: error: $ac_sub_configure failed for $ac_dir" >&2;}
    { (exit 1); exit 1; }; }
     fi
 
-    cd $ac_popdir
+    echo  "ac_popdir: $ac_popdir"
+    cd "$ac_popdir"
   done
 fi
 
